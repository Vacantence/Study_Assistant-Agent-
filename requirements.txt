# AI 学习助手依赖
# 安装: pip install -r requirements.txt

# Web UI
streamlit>=1.28,<2

# LLM Agent
# NOTE: Python 3.14+ requires langchain 1.x — 0.3.x has pydantic v1 annotations
# that crash on 3.14's annotationlib.eval(). See langchain#28114.
langchain>=1.2,<2
langchain-community>=0.4,<1
langchain-openai>=1.1,<2

# REST API
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-jose[cryptography]>=3.3,<4
python-multipart>=0.0.9,<1

# Search & Crawl
duckduckgo-search>=6,<10
trafilatura>=1.6,<2

# Document parsing
python-docx>=1.1,<2
pypdf>=4,<6

# LLM SDK (for direct API calls in tools)
openai>=1,<2

# Embedding
numpy>=1.24,<3

# Config
python-dotenv>=1,<2
